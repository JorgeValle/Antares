const assert = require('assert');
describe('The delete controller', function() {
  describe('delete one by id', function() {
    it('deletes the specific content', function() {
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  });
});
