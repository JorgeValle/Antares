const assert = require('assert');
describe('The update controller', function() {
  describe('update by id method', function() {
    it('is updating the correct item', function() {
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  });
});
