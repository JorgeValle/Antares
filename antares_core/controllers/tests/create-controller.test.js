const assert = require('assert');
describe('The create controller', function() {
  describe('create one method', function() {
    it('creates one specific content', function() {
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  });
});
