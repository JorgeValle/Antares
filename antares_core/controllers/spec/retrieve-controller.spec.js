var retrieveController = require('../retrieve.controller');


// Be descriptive with titles here. The describe and it titles combined read like a sentence.
describe('Retrieve controller', function() {
  it('is returning all by content type', function() {
    // An intentionally failing test. No code within expect() will never equal 4.
    expect(4).toEqual(4);
  });
  it('is returning one by id', function() {
    // An intentionally failing test. No code within expect() will never equal 4.
    expect(2+3).toEqual(5);
  });
});
